# WORKFLOW IS NOT WORKING. FIX IT. PROBLEM LOOKS TO BE WITH THE SECOND COMMAND AFTER THE -f TAG
# install brew then brew install openssh
# ssh-keygen -t rsa -f ~/.ssh/id_rsa <<< y
# cat ~/.ssh/id_rsa.pub
# ssh -T git@github.com
